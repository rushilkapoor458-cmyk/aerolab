"""Reference datasets and regeneration of the validation report.

Every number quoted in the validation report is produced by running the
solvers in this package against a reference dataset stored here. Nothing in
the report is transcribed by hand, so the report cannot drift away from the
code.
"""
