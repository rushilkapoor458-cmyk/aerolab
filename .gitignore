# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# Environments
.venv/
venv/

# macOS
.DS_Store

# Generated output — the validation report must regenerate from source,
# so the built artefacts are deliberately not tracked.
docs/_build/
*.pdf
!docs/reference/*.pdf

# Website production build — regenerates from source with
# `python3 website/tools/build.py`, so it is not tracked.
website/dist/
